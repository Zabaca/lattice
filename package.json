{
	"name": "@zabaca/lattice",
	"version": "1.3.9",
	"description": "Local-first retrieval engine for markdown knowledge bases",
	"type": "module",
	"bin": {
		"lattice": "dist/main.js"
	},
	"files": [
		"dist",
		"commands",
		"README.md"
	],
	"scripts": {
		"build": "bun build src/main.ts --outdir dist --target bun --packages external",
		"dev": "bun run src/main.ts",
		"test": "bun test",
		"check": "tsc --noEmit && biome check .",
		"check:fix": "tsc --noEmit && biome check --write .",
		"lattice": "bun run src/main.ts",
		"prepublishOnly": "bun run build",
		"release:patch": "bun run check && bun test && bun pm version patch && git push && git push --tags && bun publish",
		"release:minor": "bun run check && bun test && bun pm version minor && git push && git push && bun publish",
		"release:major": "bun run check && bun test && bun pm version major && git push && git push && bun publish",
		"release:dry": "bun publish --dry-run"
	},
	"keywords": [
		"knowledge-graph",
		"sqlite",
		"documentation",
		"markdown",
		"ai",
		"embeddings",
		"semantic-search"
	],
	"author": "Zabaca",
	"license": "MIT",
	"publishConfig": {
		"access": "public"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/Zabaca/lattice.git"
	},
	"dependencies": {
		"gray-matter": "^4.0.3",
		"zod": "^4.1.13"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.2.6",
		"@types/bun": "latest",
		"typescript": "^5.0.0"
	}
}
